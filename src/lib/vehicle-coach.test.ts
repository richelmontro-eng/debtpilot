import { describe,expect,it } from 'vitest'; import { coachVehicle,type FinancialContext,type VehicleScenario } from './vehicle';
const finances:FinancialContext={monthlyIncome:7000,monthlyBills:1800,monthlyDebtMinimums:500,monthlyLiving:1500,checking:5000,savings:20000,checkingCushion:2000,existingVehicleMonthly:0};
const scenario:VehicleScenario={price:25000,downPayment:5000,tradeIn:0,taxRate:6,fees:500,apr:6,termMonths:60,insuranceMonthly:120,fuelMonthly:150,maintenanceMonthly:75};
describe('vehicle affordability coach',()=>{it('confirms an affordable vehicle',()=>expect(coachVehicle(scenario,finances).affordable).toBe(true));it('returns a lower range for excessive price',()=>{const r=coachVehicle({...scenario,price:90000},finances);expect(r.affordable).toBe(false);expect(r.recommendedMaximumPrice).toBeLessThan(90000)});it('improves affordability with a higher down payment',()=>{const low=coachVehicle({...scenario,price:50000},finances),high=coachVehicle({...scenario,price:50000,downPayment:25000},finances);expect(high.monthlyPaymentDifference).toBeLessThan(low.monthlyPaymentDifference)});it('handles a cash purchase',()=>{const r=coachVehicle({...scenario,price:15000,downPayment:15000,termMonths:0},finances);expect(r.maximumLoanAmount).toBe(0);expect(r.affordable).toBe(true)});it('handles negative monthly capacity',()=>{const r=coachVehicle(scenario,{...finances,monthlyIncome:2500});expect(r.safeVehicleBudget).toBe(0);expect(r.affordable).toBe(false);expect(r.monthsToSaveDifference).toBeNull()});it('includes insurance fuel and maintenance',()=>{const low=coachVehicle({...scenario,insuranceMonthly:0,fuelMonthly:0,maintenanceMonthly:0},finances),high=coachVehicle({...scenario,insuranceMonthly:500,fuelMonthly:500,maintenanceMonthly:500},finances);expect(high.targetMonthlyPayment).toBeLessThan(low.targetMonthlyPayment)});it('calculates vehicle price backward from payment capacity',()=>{const r=coachVehicle(scenario,finances);expect(r.recommendedMaximumPrice).toBeGreaterThan(r.maximumLoanAmount);expect(r.affordablePriceLow).toBeCloseTo(r.recommendedMaximumPrice*.8)})});
